SIEM -> Security Information and Event Management
invocation -> A call in log terminology
FASTMCP-> FRAMEWORK
JSON = data format (like a container)
RPC = Remote Procedure Call (calling a function on another machine)

JSON-RPC = "I'll send you a JSON message asking you to run a function, and you send me a JSON message back with the result"

